# install the repo with mvn
mvn install

# start the server
mvn spring-boot:run


Login url: http://localhost:8080/auth/login

{
	"username": "admin",
	"password": "123"
}

{
	"username": "user",
	"password": "123"
}

